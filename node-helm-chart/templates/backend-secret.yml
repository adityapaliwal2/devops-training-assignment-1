apiVersion : v1
kind: Secret
metadata:
  name: {{ .Values.backend.secret.name}}
data:
  DB_USER: {{ .Values.backend.secret.user }}
  DB_PASSWORD: {{ .Values.backend.secret.password }}
  DB_DATABASE: {{ .Values.backend.secret.database }}  